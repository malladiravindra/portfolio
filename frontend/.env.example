# Base URL of the Django backend API (see backend/README.md).
# Used by the Contact section to submit the contact form to POST /api/contact/.
# Defaults to http://127.0.0.1:8000 if not set — fine for local development.
VITE_API_URL=http://127.0.0.1:8000
